jekyll:
    image: jekyll/jekyll:latest
    command: bash -c "gem install bundler && touch Gemfile.lock && chmod a+w Gemfile.lock && jekyll serve --force_polling"
    ports:
        - 4000:4000
    volumes:
        - .:/srv/jekyll
